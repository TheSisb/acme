{
  "name": "acme",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "1.2.14",
    "typescript": "4.7.2"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "yarn@3.2.1"
}
